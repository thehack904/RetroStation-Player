# RetroStation-Player
RetroStation Player is a lightweight, browser-controlled IPTV endpoint for a dedicated display. It loads channels from an compatible M3U playlist, lets another device select a channel, launches it fullscreen through mpv, and remembers the selected channel for automatic playback after reboot.
